import React from 'react';
import { render } from 'react-dom';
import App from './app.js';
// import App from './portfolio2/app';

const rootEl = document.getElementById('app');


render(<App />, rootEl);
